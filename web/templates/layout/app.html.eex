<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <meta name="description" content="sysashi personal webpage">
    <meta name="author" content="sysashi">

    <title><%= title(@conn, "sysashi") || "sysashi's website" %></title>

    <%= unless Mix.env == :dev do %>
    <link href="https://fonts.googleapis.com/css?family=Vollkorn" rel="stylesheet">
    <%= end %>

    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>

    <%= render "header.html", assigns %>
    <div class="container">
            <%= render @view_module,
            @view_template, assigns %>
    </div> <!-- /container -->
    <%= render "footer.html", assigns %>
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
